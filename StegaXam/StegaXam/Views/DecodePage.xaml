<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StegaXam.Views.DecodePage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:StegaXam.ViewModels"  
             xmlns:model="clr-namespace:StegaXam.Models"  
             x:Name="BrowseItemsPage">
    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Image x:Name="image" Grid.Row="0">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                </Image.GestureRecognizers>
            </Image>
            <Entry Grid.Row="1" x:Name="text" TextColor="Black" Text="Hello"/>
            <Button Text="Pick Photo" Grid.Row="2" Background="Green"
                    Clicked="OnPickPhotoButtonClicked" />

            <Button Grid.Row="3" Text="Read" Clicked="ReadClick" Background="red"/>
            <Label Grid.Row="4" x:Name="lbText" TextColor="White" Background="Blue"/>
        </Grid>
    </ScrollView>
</ContentPage>
